# PRD — ระบบส่งแคปชันให้หัวหน้าตรวจ (v1)

---

## 1. บริบท

ทุกวันนี้แคปชันถูกส่งให้หัวหน้าตรวจผ่าน Google Docs ทำให้งานเก่างานใหม่ปนกันในลิงก์เดียว หัวหน้าไม่รู้ว่าต้องอ่านอันไหน ไม่รู้ว่าแคปชันนี้ใช้คู่กับรูปไหน และพอแก้แล้วข้อความก่อนแก้ก็หายไปเลย เว็บนี้ทำให้แคปชันแต่ละชิ้นอยู่เป็นชิ้น ๆ มีสถานะชัดว่าถึงไหนแล้ว มีลิงก์รูปประกอบอยู่ข้าง ๆ และเก็บทุกเวอร์ชันไว้ให้ย้อนดูได้ว่าก่อนแก้เขียนว่าอะไร

**ประเภทแอป: แบบ B — หน้าบ้าน + หลังบ้าน** หัวหน้ากับคนในทีมเปิดลิงก์เข้ามาอ่านและกดตรวจได้เลยโดยไม่ต้องสมัคร ส่วนเจ้าของแอป (คนเขียนแคปชัน) login เข้าไปสร้างและแก้งานในหลังบ้าน

**Core loop:**
> คนเขียนแคปชัน (เจ้าของแอป) ส่งแคปชันพร้อมลิงก์รูปเข้าคิวรอตรวจได้ และหัวหน้าเปิดลิงก์เดียวเห็นคิวทั้งหมด อ่าน คอมเมนต์ แล้วกดผ่านหรือขอแก้ได้ในที่เดียว

---

## 2. ผู้ใช้และบทบาท

| ใคร | ทำอะไรได้ | ต้อง login ไหม |
|---|---|---|
| **เจ้าของแอป (คนเขียนแคปชัน)** | สร้างชิ้นงานใหม่ แก้ข้อความ ส่งตรวจ กดว่าลงแล้ว ลบชิ้นงาน | ต้อง login (บัญชีเดียว ผ่าน Neon Auth) |
| **หัวหน้า / คนตรวจ** | เปิดลิงก์เข้ามาเห็นคิวรอตรวจ อ่านแคปชัน เปิดลิงก์รูป กดผ่าน หรือกดขอแก้พร้อมพิมพ์คอมเมนต์ 1 ครั้ง | ไม่ต้อง — ใครมีลิงก์ก็เปิดได้ |
| **คนในทีมคนอื่น** | เปิดหน้าคลังงานที่ผ่านแล้ว กรองตามแบรนด์และช่องทาง เพื่อใช้เป็นตัวอย่างตอนเขียนงานใหม่ | ไม่ต้อง — ใครมีลิงก์ก็เปิดได้ |

> ⚠ **สมมติฐาน — ช่วยยืนยัน:** ใครมีลิงก์ก็กดผ่าน/ขอแก้ได้ ไม่มีรหัสผ่านกั้น ตามที่เลือกไว้ ถ้าลิงก์หลุดออกไป คนนอกจะเห็นและกดได้ด้วย

---

## 3. เส้นทางของผู้ใช้

**ฝั่งเจ้าของแอป**
1. เปิดเว็บครั้งแรก เจอหน้า login กรอกอีเมลกับรหัสผ่านของตัวเอง
2. เข้ามาเจอหน้ารวมงานทั้งหมด เรียงชิ้นใหม่สุดไว้บนสุด
3. กดปุ่ม "เพิ่มแคปชันใหม่"
4. กรอกหัวข้อสั้น ๆ เลือกแบรนด์ เลือกช่องทาง ใส่กำหนดลง ใส่ชื่อคนเขียน วางลิงก์รูป และพิมพ์ตัวแคปชัน
5. กด "บันทึกร่าง" ไว้ก่อนได้ หรือกด "ส่งตรวจ" เลย
6. พอกดส่งตรวจ สถานะเปลี่ยนเป็น "รอตรวจ" และระบบเก็บข้อความตอนนั้นไว้เป็นเวอร์ชันหนึ่ง
7. ก๊อปลิงก์หน้าคิวรอตรวจส่งให้หัวหน้าทางแชท (ลิงก์เดิมตลอด ไม่ต้องส่งใหม่ทุกครั้ง)
8. ระหว่างรอตรวจ นึกอะไรออกก็เข้าไปแก้ข้อความได้เรื่อย ๆ ไม่มีอะไรล็อก หัวหน้าจะยังเห็นเวอร์ชันที่ส่งไปแล้วเหมือนเดิม จนกว่าผมจะกด "ส่งเวอร์ชันใหม่ให้ตรวจ"
9. กลับมาเปิดดูเมื่อไหร่ก็ได้ ถ้าหัวหน้าตรวจแล้วจะเห็นสถานะเปลี่ยนเป็น "ผ่าน" หรือ "ขอแก้"
10. ถ้าเป็น "ขอแก้" จะเห็นคอมเมนต์ของหัวหน้าอยู่ใต้เวอร์ชันนั้น แก้ข้อความแล้วกด "ส่งตรวจอีกครั้ง" ระบบเก็บเป็นเวอร์ชันใหม่ ของเก่ายังอยู่ครบ
11. พอผ่านแล้วและเอาไปลงจริงแล้ว กด "ลงแล้ว" ชิ้นงานย้ายไปอยู่ในคลังงานที่ผ่านแล้ว

**ฝั่งหัวหน้า**
1. กดลิงก์ที่ได้รับมา เจอหน้าคิวรอตรวจทันที ไม่ต้อง login ไม่ต้องสมัคร
2. เห็นรายการที่รอตรวจทั้งหมด ชิ้นใหม่สุดอยู่บนสุด แต่ละบรรทัดบอกหัวข้อ แบรนด์ ช่องทาง กำหนดลง และส่งมาเมื่อไหร่
3. กดเข้าไปในชิ้นที่จะตรวจ
4. อ่านตัวแคปชัน และกดลิงก์รูปเพื่อดูว่าแคปชันนี้ประกอบรูปไหน
5. ถ้าเคยแก้มาแล้ว กดดูเวอร์ชันก่อนหน้าได้ว่าเดิมเขียนว่าอะไร และคอมเมนต์รอบที่แล้วบอกอะไรไว้
6. กด "ผ่าน" หรือกด "ขอแก้" แล้วพิมพ์คอมเมนต์ 1 ครั้งว่าต้องแก้อะไร
7. เสร็จแล้วกดกลับไปหน้าคิว ชิ้นที่ตรวจไปแล้วหายออกจากคิว เหลือแต่ที่ยังไม่ได้ตรวจ

**ฝั่งคนในทีมคนอื่น**
1. กดลิงก์เข้ามา กดเมนู "คลังงานที่ผ่านแล้ว"
2. กรองตามแบรนด์หรือช่องทาง
3. กดเข้าไปอ่านแคปชันที่ผ่านแล้ว พร้อมดูลิงก์รูปประกอบ เพื่อใช้เป็นตัวอย่าง

---

## 4. หน้าจอฝั่งผู้ใช้ (ไม่ต้อง login)

### 4.1 หน้าคิวรอตรวจ (หน้าแรกที่หัวหน้าเปิด)
**มีไว้ทำอะไร:** ให้หัวหน้าเห็นในหน้าเดียวว่าต้องตรวจอะไรบ้าง ไม่ต้องเลื่อนหา

**บนหน้ามีอะไร:**
- หัวเรื่อง "รอตรวจ" พร้อมตัวเลขจำนวนชิ้น
- รายการชิ้นงานสถานะ "รอตรวจ" ตั้งต้นเรียงชิ้นที่ใหม่สุดไว้บนสุด
- **ตัวเลือก "เรียงลำดับ"** บอกชัดว่าอันบนสุดคืออันใหม่หรืออันเก่า และสลับเป็น "เก่าสุดอยู่บน" ได้ (มีทุกหน้าที่เป็นรายการ)
- แต่ละบรรทัดแสดง: **รูปย่อของรูปประกอบ** (ถ้ามีหลายรูปจะโชว์รูปแรกพร้อมป้ายบอกจำนวน เช่น "3 รูป"), หัวข้อสั้น, ป้ายแบรนด์, ป้ายช่องทาง, กำหนดลง, ส่งมาเมื่อไหร่, เวอร์ชันที่เท่าไหร่ (ถ้าไม่มีรูปหรือรูปขึ้นไม่ได้ ก็ไม่แสดงกรอบรูปว่าง ๆ)
- เมนูไปหน้า "คลังงานที่ผ่านแล้ว"

**ปุ่มกดแล้วเกิดอะไร:**
- กดที่บรรทัดชิ้นงาน → เข้าหน้ารายละเอียดของชิ้นนั้น
- กดเมนูคลังงาน → ไปหน้า 4.3

### 4.2 หน้ารายละเอียดชิ้นงาน
**มีไว้ทำอะไร:** ให้อ่านแคปชัน ดูรูปประกอบ ดูประวัติการแก้ แล้วตัดสินใจตรวจในหน้าเดียว

**บนหน้ามีอะไร:**
- หัวข้อสั้น + ป้ายสถานะปัจจุบัน + ป้ายแบรนด์ + ป้ายช่องทาง
- กำหนดลง และชื่อคนเขียน
- ตัวแคปชันของเวอร์ชันที่ส่งตรวจล่าสุด แสดงเต็มไม่ตัดข้อความ (ไม่ใช่ข้อความที่เจ้าของกำลังแก้ค้างอยู่) **ลากคลุมข้อความส่วนไหนก็ได้เพื่อผูกโน้ตกับจุดนั้นโดยเฉพาะ**
- **รูปประกอบแสดงเป็นรูปจริงทุกรูป อยู่บนสุดของหน้า** (เห็นก่อนอ่านแคปชัน) ถ้ามีหลายรูปจะเรียงต่อกันพร้อมเลขกำกับ "รูปที่ 1, 2, 3" ถ้ารูปไหนขึ้นไม่ได้ก็ขึ้นเฉพาะรูปนั้น รูปที่เหลือยังแสดงปกติ ไม่ใช่แค่ลิงก์ ดูได้ในหน้าเลยว่าแคปชันนี้คู่กับรูปไหน กดที่รูป (หรือปุ่มใต้รูป) เพื่อเปิดต้นฉบับในแท็บใหม่ ลิงก์ Google Drive ระบบแปลงให้เองอัตโนมัติ ถ้ารูปขึ้นไม่ได้ (เช่นยังไม่ได้ตั้งแชร์เป็น "ทุกคนที่มีลิงก์ดูได้") จะบอกสาเหตุและให้กดลิงก์เปิดดูแทน
- กล่อง "ประวัติเวอร์ชันทั้งหมด" วางเต็มความกว้างด้านล่างสุดของหน้า (ไม่ใช่คอลัมน์แคบข้าง ๆ จะได้อ่านออก) ไล่จากเวอร์ชันใหม่สุดลงไปเวอร์ชันแรก แต่ละเวอร์ชันแสดงข้อความตอนนั้น (พร้อมไฮไลต์ส่วนที่มีโน้ตชี้จุด) วันเวลาที่ส่ง ผลการตรวจ และโน้ตของคนตรวจ (ถ้ามี)
- ช่องพิมพ์โน้ต — เพิ่มได้หลายโน้ตต่อการตรวจ 1 ครั้ง แต่ละโน้ตจะ "ชี้จุด" (ผูกกับข้อความที่ลากคลุมไว้) หรือเป็น "โน้ตรวม" (ไม่ชี้จุด) ก็ได้
- **แท็บสลับดูเนื้อหา** บนกล่องแคปชันใหญ่ฝั่งซ้าย — ทุกอย่างที่เป็นการ "อ่าน" รวมอยู่ที่เดียว ส่วนคอลัมน์ขวาเหลือแต่เรื่อง "ตรวจงาน"
  - `เวอร์ชันล่าสุด` — ข้อความที่ต้องตรวจ ลากคลุมสั่งแก้ได้เฉพาะแท็บนี้
  - `สิ่งที่เปลี่ยนไป` (มีเฉพาะเวอร์ชันที่ 2 ขึ้นไป) — เทียบกับเวอร์ชันก่อนหน้าทีละบรรทัด สีเขียว = บรรทัดที่แก้มาใหม่, ขีดฆ่าสีเทา = ของเดิมที่ถูกแทนที่หรือลบออก พร้อมสรุปจำนวนบรรทัดที่เปลี่ยน
  - `เวอร์ชัน N` ของเก่าแต่ละเวอร์ชัน — อ่านย้อนหลังได้เต็มความกว้างเท่าเวอร์ชันปัจจุบัน พร้อมไฮไลต์และโน้ตของรอบนั้น
- **กล่อง "รอบที่แล้วคุณขอแก้ N จุด"** (แสดงเฉพาะเวอร์ชันที่ 2 ขึ้นไป) — ระบบเทียบให้เองว่าโน้ตที่สั่งไว้รอบก่อนข้อไหน "แก้แล้ว" (ประโยคเดิมหายไปจากข้อความใหม่) และข้อไหน "ยังไม่ได้แก้" (ประโยคเดิมยังอยู่เหมือนเดิมเป๊ะ) จุดที่ยังไม่ได้แก้จะระบายสีแดงไว้ในตัวแคปชันด้วย หัวหน้าจะได้ไม่ต้องไล่อ่านเทียบเองทุกรอบ ส่วนโน้ตรวมที่ไม่ได้ชี้ประโยคไหน ระบบเดาแทนไม่ได้ จะขึ้นว่า "ตรวจเอง"
- ปุ่ม "ผ่าน" และปุ่ม "ขอแก้"

**ปุ่มกดแล้วเกิดอะไร:**
- กด "ผ่าน" → สถานะเปลี่ยนเป็น "ผ่าน" บันทึกวันเวลาที่ตรวจไว้กับเวอร์ชันนั้น ชิ้นงานหายออกจากคิวรอตรวจ
- กด "ขอแก้" → ถ้ายังไม่มีโน้ตแม้แต่อันเดียว (ชี้จุดหรือรวมก็ได้) ระบบเตือนให้เพิ่มก่อน เมื่อเพิ่มแล้วกดได้ สถานะเปลี่ยนเป็น "ขอแก้" โน้ตทั้งหมดผูกติดเวอร์ชันนั้นไปตลอด ชิ้นงานหายออกจากคิวรอตรวจ
- กดลิงก์รูป → เปิดแท็บใหม่
- ถ้าชิ้นงานไม่ได้อยู่สถานะ "รอตรวจ" ปุ่มผ่าน/ขอแก้จะไม่แสดง เห็นแต่เนื้อหาและประวัติ

### 4.3 หน้าคลังงานที่ผ่านแล้ว
**มีไว้ทำอะไร:** ให้คนในทีมย้อนดูแคปชันที่ผ่านแล้วเพื่อใช้เป็นตัวอย่าง

**บนหน้ามีอะไร:**
- ตัวกรองแบรนด์ และตัวกรองช่องทาง (เลือกจากรายการตายตัว ไม่มีช่องพิมพ์เอง)
- รายการชิ้นงานสถานะ "ผ่าน" และ "ลงแล้ว" ตั้งต้นเรียงใหม่สุดบนสุด สลับเป็นเก่าสุดอยู่บนได้ด้วยตัวเลือก "เรียงลำดับ"
- แต่ละบรรทัดแสดงหัวข้อสั้น ป้ายแบรนด์ ป้ายช่องทาง สถานะ และกำหนดลง

**ปุ่มกดแล้วเกิดอะไร:**
- เลือกตัวกรอง → รายการกรองทันทีในหน้าเดิม
- กดที่บรรทัด → เข้าหน้ารายละเอียด (หน้า 4.2 แบบไม่มีปุ่มตรวจ — หน้าตาและแท็บเหมือนตอนกำลังตรวจทุกอย่าง ต่างแค่ไม่มีกล่องตรวจ)

---

## 5. หน้าจอฝั่งเจ้าของ (ต้อง login)

### 5.1 หน้ารวมงานทั้งหมด
**เห็นอะไร:** ชิ้นงานทุกสถานะในรายการเดียว **เรียงชิ้นที่มีความเคลื่อนไหวล่าสุดไว้บนสุด** เพราะ v1 ไม่มีการแจ้งเตือน เจ้าของต้องเปิดมาแล้วเห็นของใหม่ทันที

- แถวบนสุดของหน้ามีแถบสรุปจำนวนแต่ละสถานะ: ร่าง / รอตรวจ / ขอแก้ / ผ่าน / ลงแล้ว
- ชิ้นที่สถานะ "ขอแก้" ให้มีป้ายเด่นกว่าสถานะอื่น เพราะเป็นงานที่ต้องลงมือทำต่อ
- ตัวกรองสถานะ แบรนด์ ช่องทาง
- ปุ่ม "เพิ่มแคปชันใหม่"
- ปุ่มก๊อปลิงก์หน้าคิวรอตรวจ สำหรับส่งให้หัวหน้า

### 5.2 หน้าเพิ่ม / แก้แคปชัน
**จัดการอะไรได้:**
- หัวข้อสั้น (พิมพ์เอง)
- แบรนด์ (ติ๊กจากรายการตายตัว **เลือกได้หลายอัน** ต้องเลือกอย่างน้อย 1 อันก่อนส่งตรวจ)
- ช่องทาง (เลือกจากรายการตายตัว)
- กำหนดลง (เลือกวันที่)
- ชื่อคนเขียน (พิมพ์เอง)
- ลิงก์รูปประกอบ — **ใส่ได้หลายรูป** ช่องละ 1 ลิงก์ มีปุ่ม "+ เพิ่มรูป" และปุ่มลบรายรูป พร้อมรูปย่อข้างช่องให้เห็นทันทีว่าวางลิงก์ถูกอันไหม (เว้นว่างได้)
- ตัวแคปชัน (ช่องข้อความยาว)

**ตอนเข้ามาแก้ชิ้นงานที่หัวหน้าตรวจแล้ว (ช่วยให้คนแก้ทำงานง่ายขึ้น):**
- ช่องพิมพ์แคปชันจะระบายสีเหลืองตรงประโยคที่หัวหน้าขอให้แก้ พร้อมเลขกำกับ 1, 2, 3 เหมือนที่เห็นในหน้ารายละเอียด
- ข้าง ๆ ช่องพิมพ์มีแถบโน้ตของหัวหน้าติดหน้าจอไว้ เลื่อนพิมพ์ยังไงก็ยังเห็น
- กดที่ประโยคในแถบโน้ต → ช่องพิมพ์เลื่อนไปหาประโยคนั้นและคลุมข้อความให้ พิมพ์ทับได้ทันที
- พอแก้ข้อความตรงนั้นแล้ว โน้ตข้อนั้นจะจางลงและขึ้นว่า "แก้ข้อความตรงนี้ไปแล้ว" และติ๊ก "แก้แล้ว" เองได้ กันตกหล่น

**ปุ่มกดแล้วเกิดอะไร:**
- "บันทึกไว้ก่อน" → บันทึกข้อความไว้เฉย ๆ ไม่เปลี่ยนสถานะ ถ้าเป็นชิ้นใหม่สถานะเป็น "ร่าง" ถ้าชิ้นนั้นอยู่สถานะ "รอตรวจ" อยู่แล้ว หัวหน้าจะยังเห็นข้อความเดิมที่ส่งไป และหน้าของเจ้าของขึ้นป้าย "มีการแก้ที่ยังไม่ได้ส่งตรวจ"
- "ส่งตรวจ" → เก็บข้อความตอนนั้นเป็นเวอร์ชันใหม่ สถานะเปลี่ยนเป็น "รอตรวจ" และชิ้นงานโผล่บนสุดของคิวหัวหน้า
- "ยกเลิก" → กลับหน้ารวมโดยไม่บันทึก

### 5.3 หน้ารายละเอียดชิ้นงาน (ฝั่งเจ้าของ)
**เห็นอะไร:** เหมือนหน้า 4.2 ทุกอย่าง บวกปุ่มจัดการ และถ้ามีการแก้ที่ยังไม่ได้ส่งตรวจ จะมีแถบขึ้นบอกว่า "แก้แล้วแต่ยังไม่ได้ส่งตรวจ — ตอนนี้หัวหน้ายังเห็นเวอร์ชันก่อนหน้าอยู่" พร้อมปุ่มดูข้อความที่แก้ค้างไว้

**ปุ่มกดแล้วเกิดอะไร:**
- "แก้ไข" → เข้าหน้า 5.2 พร้อมข้อมูลเดิม แก้ได้ทุกสถานะ ไม่มีการล็อก
- "ส่งเวอร์ชันใหม่ให้ตรวจ" (แสดงเมื่อสถานะเป็น "ร่าง" หรือ "ขอแก้" หรือเมื่อมีการแก้ที่ยังไม่ได้ส่งตรวจ) → เก็บข้อความปัจจุบันเป็นเวอร์ชันใหม่ สถานะเป็น "รอตรวจ" และชิ้นงานเด้งขึ้นบนสุดของคิวหัวหน้าอีกครั้ง
- "ลงแล้ว" (แสดงเมื่อสถานะเป็น "ผ่าน") → สถานะเปลี่ยนเป็น "ลงแล้ว"
- "ลบ" → ถามยืนยันก่อน 1 ครั้ง แล้วซ่อนชิ้นงานออกจากทุกหน้า

---

## 6. ข้อมูลที่ต้องเก็บ

**ต่อแคปชัน 1 ชิ้น** เราเก็บหัวข้อสั้น ๆ, แบรนด์ (เก็บได้หลายอัน), ช่องทางที่จะลง, กำหนดลง, ชื่อคนเขียน, ลิงก์รูปประกอบ, สถานะปัจจุบัน, วันเวลาที่สร้าง, วันเวลาที่มีความเคลื่อนไหวล่าสุด และข้อความที่เจ้าของกำลังแก้ค้างอยู่ตอนนี้ (ข้อความที่ยังไม่ได้ส่งตรวจ)

**ต่อเวอร์ชัน 1 เวอร์ชัน** เราเก็บตัวข้อความแคปชันตอนนั้นแบบเต็ม ๆ, เลขเวอร์ชันที่เท่าไหร่, วันเวลาที่ส่งตรวจ, ผลการตรวจ (ยังไม่ตรวจ / ผ่าน / ขอแก้) และวันเวลาที่ตรวจ (ไม่เก็บชื่อคนตรวจ)

**ต่อโน้ต 1 อัน** (1 เวอร์ชันมีโน้ตได้หลายอัน) เราเก็บข้อความที่ลากคลุมไว้ในแคปชัน (เว้นว่างได้ถ้าเป็นโน้ตรวม ไม่ชี้เฉพาะจุด), ข้อความโน้ตที่หัวหน้าพิมพ์ และวันเวลาที่สร้างโน้ตนั้น

แคปชัน 1 ชิ้นมีได้หลายเวอร์ชัน ข้อความเก่าไม่เคยถูกทับ ทุกครั้งที่ส่งตรวจใหม่คือเพิ่มเวอร์ชันใหม่ต่อท้าย

**รายการตายตัวที่ต้องใส่ไว้ในระบบก่อน** คือรายชื่อแบรนด์และรายชื่อช่องทาง ทั้งสองอย่างเลือกจากรายการเท่านั้น ห้ามมีช่องให้พิมพ์เอง เพราะถ้าพิมพ์อิสระแล้วสะกดต่างกันจะย้อนหาไม่เจอ

---

## 7. กฎและกรณีพิเศษ

- ส่งตรวจไม่ได้ถ้ายังไม่ได้กรอก: หัวข้อสั้น, แบรนด์, ช่องทาง, ตัวแคปชัน — ระบบขึ้นข้อความบอกว่าขาดอะไร ตรงช่องนั้นเลย
- ลิงก์รูปและกำหนดลง เว้นว่างได้ ถ้าเว้นว่างให้แสดงว่า "ยังไม่ได้ใส่ลิงก์รูป" / "ยังไม่กำหนด"
- กด "ขอแก้" โดยไม่เพิ่มโน้ตแม้แต่อันเดียวไม่ได้ ระบบบังคับให้เพิ่มก่อน (จะชี้จุดหรือเป็นโน้ตรวมก็ได้) เพราะทั้งระบบนี้ทำขึ้นมาเพื่อไม่ให้เหตุผลการแก้หายไป
- กด "ผ่าน" ไม่ต้องเพิ่มโน้ตก็ได้
- หัวหน้าลากคลุมข้อความในแคปชันแล้วพิมพ์โน้ตผูกกับจุดนั้นได้ เพิ่มได้หลายจุดในการตรวจ 1 ครั้ง เจ้าของจะเห็นข้อความส่วนนั้นถูกไฮไลต์พร้อมโน้ตกำกับ ไม่ต้องไล่หาเองว่าโน้ตพูดถึงประโยคไหน
- 1 เวอร์ชันตรวจซ้ำไม่ได้ (ไม่ว่าจะมีกี่โน้ต) ถ้าจะตรวจใหม่ต้องให้เจ้าของส่งตรวจเป็นเวอร์ชันใหม่ก่อน
- ถ้าหัวหน้าเปิดหน้าค้างไว้แล้วเจ้าของส่งเวอร์ชันใหม่เข้ามาพอดี พอหัวหน้ากดตรวจ ระบบจะบอกว่า "ชิ้นงานนี้มีเวอร์ชันใหม่กว่าแล้ว กรุณารีเฟรช" แล้วไม่บันทึกผลทับ
- ถ้าหัวหน้าสองคนกดพร้อมกัน ใครกดถึงก่อนถือว่าอันนั้นมีผล อีกคนจะเห็นข้อความว่าชิ้นนี้ถูกตรวจไปแล้ว
- เจ้าของแก้ข้อความได้ทุกสถานะ ตลอดเวลา ไม่มีการล็อก การแก้ไม่สร้างเวอร์ชันใหม่ เวอร์ชันใหม่เกิดตอนกด "ส่งเวอร์ชันใหม่ให้ตรวจ" เท่านั้น
- ระหว่างสถานะ "รอตรวจ" ถ้าเจ้าของแก้ข้อความ หัวหน้ายังเห็นข้อความของเวอร์ชันที่ส่งไปแล้ว ไม่ใช่ข้อความที่แก้ค้างอยู่ เพราะไม่งั้นคอมเมนต์จะไปผูกกับข้อความคนละอันกับที่หัวหน้าอ่าน
- ถ้าแก้ค้างไว้แล้วหัวหน้ากดผ่านพอดี ผลตรวจจะผูกกับเวอร์ชันที่หัวหน้าอ่านเท่านั้น ข้อความที่แก้ค้างไว้ยังอยู่ครบไม่หาย และหน้าของเจ้าของจะยังขึ้นป้ายเตือนว่ามีของแก้ค้างที่ยังไม่ได้ส่งตรวจ
- ถ้าแก้ค้างไว้แต่ไม่เคยกดส่ง ข้อความที่แก้จะไม่ถือว่าเป็นเวอร์ชัน และไม่ขึ้นในประวัติเวอร์ชัน
- ลบแล้วไม่หายถาวร แค่ซ่อนออกจากทุกหน้า ถ้าลบผิดต้องบอกคนทำระบบให้ดึงกลับ (v1 ยังไม่มีหน้าถังขยะ)
- หน้าคิวรอตรวจตอนไม่มีงาน ให้ขึ้นว่า "ตอนนี้ไม่มีงานรอตรวจ" ไม่ใช่หน้าขาว ๆ
- หน้ารวมงานของเจ้าของตอนยังไม่มีอะไรเลย ให้ขึ้นว่า "ยังไม่มีแคปชัน กดปุ่มเพิ่มแคปชันใหม่เพื่อเริ่ม" พร้อมปุ่ม
- คลังงานที่ผ่านแล้วตอนกรองแล้วไม่เจอ ให้ขึ้นว่า "ไม่เจองานที่ตรงกับตัวกรองนี้"
- วันและเวลาทั้งหมดใช้เวลาไทย (Asia/Bangkok) แสดงเป็นรูปแบบไทย เช่น 19 ก.ย. 2569 14:30
- หน้าเว็บทั้งหมดเป็นภาษาไทย
- ลิงก์ที่หัวหน้าเปิดเป็นลิงก์เปิดสาธารณะ ใครได้ลิงก์ไปก็อ่านและกดตรวจได้ ส่วนหน้าของเจ้าของต้อง login เสมอ ถ้าไม่ได้ login แล้วพยายามเข้า จะถูกส่งกลับไปหน้า login
- มีบัญชี login ได้บัญชีเดียวคือของเจ้าของ ไม่มีหน้าสมัครสมาชิกในเว็บ

---

## 8. สิ่งที่ต้องเตรียมก่อนเริ่ม build

- [x] **รายชื่อแบรนด์** — FutureSkill, SkillPass, Creator & Marketing, Self Growth, AI Automation Tech, Data, Business Leader
  > อยู่ในช่องเดียวกันทั้งหมด และ **1 ชิ้นงานติ๊กได้มากกว่า 1 อัน** (เช่น FutureSkill + Data) ป้ายจะขึ้นครบทุกอันที่เลือก ตัวกรองแบรนด์จะกรองแบบ "มีแบรนด์นี้อยู่ในนั้น"
- [x] **รายชื่อช่องทาง** — Facebook
  > ⚠ สมมติฐาน — ช่วยยืนยัน: มีช่องทางเดียวก็ยังทำเป็นตัวเลือกไว้ เพื่อให้เพิ่มทีหลังได้โดยไม่ต้องรื้อ
- [x] **ชุดสถานะ** — ร่าง → รอตรวจ → ขอแก้ → ผ่าน → ลงแล้ว
- [ ] ❌ **ข้อความบนหน้าเว็บ** — ยังไม่มี ต้องการ: ชื่อเว็บที่จะขึ้นหัวหน้าเว็บ และคำอธิบาย 1 บรรทัดบนหน้าคิวรอตรวจ (ระหว่างนี้จะใช้ "คิวตรวจแคปชัน" ไปก่อน)
- [ ] ❌ **แคปชันตัวอย่าง 2–3 ชิ้นของจริง** — ยังไม่มี ต้องใช้ใส่ตอนทดสอบ เพื่อดูว่าข้อความยาวจริงแสดงผลสวยไหม ขอเป็นของที่เคยผ่านแล้ว 1 ชิ้น และของที่เคยโดนขอแก้ 1 ชิ้นพร้อมคอมเมนต์ที่หัวหน้าเคยให้
- [ ] ❌ **ลิงก์รูปตัวอย่าง 1–2 ลิงก์** — ยังไม่มี ต้องรู้ว่าปกติลิงก์รูปมาจากที่ไหน (Google Drive / Canva / อื่น ๆ) เพื่อทดสอบว่ากดแล้วเปิดได้จริง
- [ ] ❌ **อีเมลที่จะใช้ login** — ยังไม่มี ต้องใช้สร้างบัญชีเจ้าของ 1 บัญชี
- [x] **รูปภาพบนหน้าเว็บ** — ไม่ต้องเตรียม v1 ไม่มีการอัปโหลดรูป ใช้วางลิงก์อย่างเดียว

---

## 9. v1 ถือว่าเสร็จเมื่อ…

1. ผม login เข้าหลังบ้านได้ และพอเปิดลิงก์เว็บในโหมดไม่ล็อกอิน ผมเข้าหน้าหลังบ้านไม่ได้
2. ผมเพิ่มแคปชันใหม่พร้อมลิงก์รูป เลือกแบรนด์กับช่องทางจากรายการ แล้วกดส่งตรวจ — ชิ้นงานโผล่บนสุดของหน้าคิวรอตรวจทันที
3. ผมเปิดลิงก์หน้าคิวในเครื่องอื่นหรือโหมดไม่ล็อกอิน (แกล้งเป็นหัวหน้า) แล้วเห็นชิ้นงานนั้น อ่านแคปชันได้เต็ม ๆ และกดลิงก์รูปแล้วเปิดรูปได้จริง จากนั้นผมกลับไปแก้ข้อความในหลังบ้าน รีเฟรชหน้าฝั่งหัวหน้า — ยังเห็นข้อความเดิมที่ส่งไป และหลังบ้านขึ้นป้ายว่ามีการแก้ที่ยังไม่ได้ส่งตรวจ
4. ผมกด "ขอแก้" โดยไม่เพิ่มโน้ต ระบบไม่ให้กด พอลากคลุมข้อความบางส่วนแล้วพิมพ์โน้ตอย่างน้อย 1 จุดก็กดได้ และสถานะเปลี่ยนเป็น "ขอแก้"
5. ผมกลับเข้าหลังบ้าน เห็นข้อความที่ถูกไฮไลต์พร้อมโน้ตของหัวหน้า รู้ทันทีว่าต้องแก้ตรงไหน แก้ข้อความแล้วกดส่งตรวจอีกครั้ง — กลายเป็นเวอร์ชัน 2 และเวอร์ชัน 1 ยังอ่านได้อยู่พร้อมโน้ตเดิมครบ
6. ผมกด "ผ่าน" แล้วกด "ลงแล้ว" — ชิ้นงานหายจากคิวรอตรวจ และไปโผล่ในหน้าคลังงานที่ผ่านแล้ว
7. ผมกรองคลังงานด้วยแบรนด์และช่องทาง แล้วเจอเฉพาะชิ้นที่ตรงกัน และตอนไม่มีข้อมูลทุกหน้าขึ้นข้อความบอก ไม่ใช่หน้าว่าง ๆ

---

## 10. ไม่ทำใน v1 (parking lot สำหรับ v2)

- **ระบบสมาชิกหลายคน + สิทธิ์ตามบทบาท (ใครส่งได้ ใครอนุมัติได้)** — เป็นระบบใหญ่อีกระบบหนึ่ง v1 ใช้ช่อง "คนเขียน" กับ "ชื่อคนตรวจ" แบบพิมพ์เองไปก่อน
- ~~ไฮไลต์ข้อความแล้วคอมเมนต์ตรงจุดแบบ Google Docs~~ — **ทำแล้ว** (เพิ่มเข้ามาหลัง v1 เดิม ตามที่ขอ): หัวหน้าลากคลุมข้อความในแคปชันแล้วพิมพ์โน้ตผูกกับจุดนั้นได้ เพิ่มได้หลายจุด เจ้าของเห็นข้อความถูกไฮไลต์พร้อมโน้ตกำกับ
- **บันทึกว่าใครเป็นคนตรวจ** — v1 มีหัวหน้าคนเดียว ไม่ต้องพิมพ์ชื่อทุกครั้ง ถ้าวันหนึ่งมีคนตรวจหลายคนค่อยกลับมาทำพร้อมระบบสมาชิก
- **คุยโต้ตอบกันไปมาในชิ้นงาน (thread หลายรอบ)** — ยังไม่ทำ การตรวจแต่ละเวอร์ชันยังจบในครั้งเดียว (เพิ่มโน้ตได้หลายจุดในการตรวจ 1 ครั้ง แต่ตรวจซ้ำเวอร์ชันเดิมไม่ได้) ถ้าจะให้หัวหน้าตอบกลับอีกต้องรอเจ้าของส่งเวอร์ชันใหม่ก่อน
- **แจ้งเตือนทุกแบบ (email, LINE, SMS)** — กติกาคลาส แทนด้วยหน้าคิวและหน้ารวมที่เรียงใหม่สุดบนสุด
- **อัปโหลดรูปในแอป** — กติกาคลาส v1 วางลิงก์รูปเอา
- ~~เทียบข้อความเวอร์ชันเก่ากับใหม่แบบไฮไลต์จุดที่ต่าง~~ — **ทำแล้ว** (เพิ่มหลัง v1 เดิม ตามที่ขอ): แท็บ "สิ่งที่เปลี่ยนไป" เทียบทีละบรรทัด เขียว = ที่แก้มาใหม่ ขีดฆ่า = ของเดิม พร้อมเช็กลิสต์บอกว่าโน้ตที่สั่งไว้รอบก่อนข้อไหนแก้แล้ว/ยังไม่ได้แก้
- **ค้นหาด้วยการพิมพ์คำ** — v1 ใช้ตัวกรองแบรนด์/ช่องทาง และรายการเรียงตามเวลา
- **export ไฟล์ / import ไฟล์** — กติกาคลาส
- **dashboard กราฟสรุป เช่น เฉลี่ยกี่รอบถึงผ่าน** — น่าสนใจแต่ไม่ได้ทำให้ core loop เกิดขึ้น
- **หน้าถังขยะกู้คืนงานที่ลบ** — v1 แค่ซ่อน ไม่ลบจริง ยังดึงกลับได้ถ้าจำเป็น
- **หลายแบรนด์/หลายทีมแยกพื้นที่กัน** — กติกาคลาส
- **ปฏิทินดูว่าชิ้นไหนกำหนดลงวันไหน** — v1 แสดงกำหนดลงเป็นข้อความในรายการก็พอ

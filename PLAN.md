# แผนงาน v1 — ระบบคิวตรวจแคปชัน (CaptionQueue)

## Context

ตอนนี้โปรเจกต์มีแค่เอกสาร (`PRD.md`, `DESIGN.md`, `design-ui/*`) ยังไม่มีโค้ดเลย ผู้ใช้ขอให้แตกงาน v1 เป็นเฟสที่ทำทีละเฟส คอมมิตได้ทีละเฟส และรอ OK ก่อนเริ่มเฟสถัดไป จุดประสงค์คือให้คนที่ไม่ใช่โปรแกรมเมอร์ตามงานได้ทุกจุด เห็นผลจริงที่คลิกได้ทุกเฟส และให้ครอบคลุมเช็กลิสต์ "v1 ถือว่าเสร็จเมื่อ…" ทั้ง 7 ข้อใน PRD ครบ

**สิ่งที่พบระหว่างอ่านเอกสาร ที่ต้องแจ้งผู้ใช้ก่อนเริ่มสร้าง (mockup มีของที่ไม่อยู่ใน PRD v1):**
mockup ใน `design-ui/` มีของหลายอย่างที่ PRD v1 ไม่ได้ขอ ตาม CLAUDE.md ("ถ้ามอคอัพมีอะไรที่ไม่อยู่ใน PRD ไม่ต้องสร้าง บอกผู้ใช้") จะ**ไม่สร้าง**สิ่งต่อไปนี้ในเฟสด้านล่าง:
- ช่องค้นหาด้วยคำ (search box) — PRD ระบุไว้ชัดใน "ไม่ทำใน v1"
- ตัวเลือกช่องทาง TikTok / LinkedIn ในฟอร์ม — PRD มีแค่ Facebook ช่องทางเดียว
- การแบ่งหน้า (pagination เลข 1 2 3 …) — PRD ไม่ได้พูดถึง ข้อมูล v1 มีไม่เยอะ ใช้รายการยาวหน้าเดียวพอ
- แผงพรีวิวโพสต์แบบเลียนแบบ Facebook Feed สด ๆ ในหน้าเพิ่ม/แก้แคปชัน — เป็นของแต่งสวยงาม ไม่ใช่ฟีเจอร์ที่ PRD ขอ
- ปุ่มแทรกคำสำเร็จรูป (ไฮไลต์ / หัวข้อย่อย / ลิงก์ CTA / แฮชแท็กหลัก) ในกล่องพิมพ์แคปชัน — เป็นเครื่องมือช่วยพิมพ์ที่ PRD ไม่ได้ขอ
- "เช็กลิสต์ความสมบูรณ์" แบบมีเปอร์เซ็นต์ และข้อความ "ระบบบันทึกร่างอัตโนมัติเมื่อ x นาทีที่แล้ว" — ของแต่งเสริม ไม่ใช่ของที่ PRD ขอ
- รูปโปรไฟล์ผู้ใช้ที่มุมขวาบน และคอมเมนต์แบบหลายคน ("Peer Reviewer") — PRD มีหัวหน้าคนเดียว ไม่เก็บชื่อคนตรวจ

ส่วนหน้าตา สี ระยะห่าง ตัวอักษร (layout, badge, การ์ด, timeline เวอร์ชัน) จะยึดตามมอคอัพและ `DESIGN.md` เต็มที่ เอามาสร้างด้วย Tailwind + shadcn/ui ไม่ก็อปโค้ด HTML ดิบ

**ของที่ PRD ยังไม่ได้เตรียม (หัวข้อ 8) แต่ไม่ใช่ตัวบล็อกการวางแผน — จะขอผู้ใช้ตอนถึงเฟสที่เกี่ยวข้อง:**
ชื่อเว็บ/คำอธิบาย 1 บรรทัด, แคปชันตัวอย่างจริง 2-3 ชิ้น, ลิงก์รูปตัวอย่าง, อีเมล login ของเจ้าของ — เฟส 1 จะใช้ข้อมูลตัวอย่างสมมติไปก่อน (คล้ายมอคอัพ) เฟสหลัง ๆ ที่ต้องใช้ของจริงจะถามตอนถึงเฟสนั้น

## โครงสร้างฐานข้อมูล (สรุปคร่าว ๆ ไว้ก่อน จะอธิบายเป็นตารางไทยให้ผู้ใช้ OK อีกทีตอนเริ่มเฟส 2)

- ตาราง `captions` — 1 แถวต่อแคปชัน 1 ชิ้น: หัวข้อสั้น, แบรนด์, ช่องทาง, กำหนดลง, ชื่อคนเขียน, ลิงก์รูป, สถานะปัจจุบัน, ข้อความที่แก้ค้างไว้ยังไม่ส่งตรวจ, วันที่สร้าง, วันที่มีความเคลื่อนไหวล่าสุด
- ตาราง `caption_versions` — 1 แถวต่อเวอร์ชัน: อ้างอิงถึงชิ้นงาน, เลขเวอร์ชัน, ข้อความเต็มตอนนั้น, ผลตรวจ, คอมเมนต์คนตรวจ, วันที่ส่งตรวจ, วันที่ตรวจ
- รายชื่อแบรนด์ (FutureSkill, SkillPass) และช่องทาง (Facebook) เก็บเป็นค่าคงที่ตายตัวในโค้ด (enum) ไม่ใช่ตารางแยก เพราะยังไม่มีความจำเป็นต้องแก้ผ่านหน้าเว็บ

## เฟสการทำงาน

### Phase 0 — เปิดโปรเจกต์ วางโครง และธีมดีไซน์
**Goal (ภาษาไทยง่าย ๆ):** เตรียมพื้นที่ทำงานว่างเปล่าให้พร้อม ใส่โทนสีและฟอนต์ตามที่ออกแบบไว้ ยังไม่มีหน้าจอจริง

**How to test:**
1. เปิดเว็บที่ localhost จะเห็นหน้าเปล่า พื้นหลังสีตามธีม (ม่วง-น้ำเงินอ่อน) ตัวอักษร IBM Plex Sans / Thai
2. ไม่มีอะไรให้กดเพราะยังไม่มีหน้าจอจริง — ถือว่าผ่านถ้าเว็บขึ้นได้ไม่มี error สีแดงเต็มจอ

**Done when:**
- โปรเจกต์ Next.js + TypeScript + Tailwind + shadcn/ui รันได้บนเครื่อง
- ใส่โทนสี/ตัวอักษร/ระยะห่างจาก `DESIGN.md` ลงใน Tailwind theme (`globals.css`)
- `npm run lint` และ `npm run build` ผ่าน
- ยังไม่แตะฐานข้อมูล ยังไม่ต้อง .env.local

**Status:** [x] done

---

### Phase 1 — หน้าจอทั้งหมดพร้อมข้อมูลตัวอย่าง (หน้าตาให้ตรงมอคอัพ)
**Goal:** สร้างหน้าตาเว็บทุกหน้าให้เหมือนภาพตัวอย่างที่ออกแบบไว้ก่อน โดยยังใช้ข้อมูลปลอมทั้งหมด กดอะไรยังไม่มีผลจริง

**หน้าที่สร้าง:** หน้าคิวรอตรวจ, หน้ารายละเอียดชิ้นงาน (ฝั่งหัวหน้า), หน้ารวมงานของเจ้าของ, หน้าเพิ่ม/แก้แคปชัน, หน้าคลังงานที่ผ่านแล้ว — ตัดของที่ระบุไว้ในหัวข้อ Context ออก (ไม่มีช่องค้นหา, ไม่มี TikTok/LinkedIn, ไม่มีปุ่มแบ่งหน้า, ไม่มีปุ่มแทรกคำสำเร็จรูป)

**How to test:**
1. เปิดหน้าแรก เห็นหน้าคิวรอตรวจ มีรายการตัวอย่าง 3-5 ชิ้น หน้าตาเหมือนภาพ `design-ui/review_queue`
2. คลิกที่ชิ้นงานหนึ่ง เข้าหน้ารายละเอียด เห็นแคปชันเต็ม ลิงก์รูป ประวัติเวอร์ชัน ปุ่มผ่าน/ขอแก้ (กดแล้วยังไม่มีอะไรเกิดขึ้นจริง)
3. เปิดเมนู "จัดการแคปชัน" เห็นหน้ารวมงานของเจ้าของ มีแถบสรุปจำนวนสถานะ และปุ่มเพิ่มแคปชันใหม่
4. กดปุ่มเพิ่มแคปชันใหม่ เห็นฟอร์มครบ (หัวข้อ, แบรนด์, ช่องทาง, กำหนดลง, ชื่อคนเขียน, ลิงก์รูป, กล่องข้อความแคปชัน)
5. เปิดเมนู "คลังงานที่ผ่านแล้ว" เห็นตัวกรองแบรนด์/ช่องทาง และรายการตัวอย่าง

**Done when:**
- ทุกหน้าใช้ข้อมูลตัวอย่าง (mock data ในโค้ด ไม่ใช่ฐานข้อมูลจริง)
- หน้าตา สี ระยะห่าง ตรงกับ `design-ui/` และ `DESIGN.md` (เทียบด้วยตาหลังสร้างเสร็จ)
- `npm run lint`, `npm run build` ผ่าน

**Status:** [x] done

---

### Phase 2 — ตั้งฐานข้อมูลจริง + หน้ารวมงานของเจ้าของ + เพิ่มแคปชันใหม่ใช้งานได้จริง
**Goal:** เริ่มเก็บแคปชันลงฐานข้อมูลจริง กดเพิ่มแคปชันใหม่แล้วบันทึกร่างหรือส่งตรวจได้จริง เห็นในหน้ารวมงาน

**How to test:**
1. เปิดหน้ารวมงาน (ยังไม่มีอะไรเลย) เห็นข้อความ "ยังไม่มีแคปชัน กดปุ่มเพิ่มแคปชันใหม่เพื่อเริ่ม"
2. กดเพิ่มแคปชันใหม่ กรอกข้อมูลให้ครบ กดส่งตรวจ — ถ้าลืมกรอกหัวข้อ/แบรนด์/ช่องทาง/ตัวแคปชัน ระบบจะขึ้นเตือนตรงช่องนั้น ไม่ให้ส่ง
3. กรอกครบแล้วกดส่งตรวจ กลับมาหน้ารวมงาน เห็นชิ้นงานใหม่อยู่บนสุด สถานะ "รอตรวจ"
4. ลองกดเพิ่มอีกชิ้น กด "บันทึกไว้ก่อน" แทน — เห็นสถานะเป็น "ร่าง"

**Done when:**
- อธิบายตาราง `captions` และ `caption_versions` เป็นตารางภาษาไทยให้ผู้ใช้ดูและตอบ OK ก่อนรัน `drizzle-kit push`
- เขียน `.env.local` เอง (ไม่ถามผู้ใช้ ไม่โชว์ค่าจริงในแชท)
- validate ฝั่ง server ครบตามกฎข้อ 7 ของ PRD
- `npm run lint`, `npm run build` ผ่าน มีข้อมูลทดสอบในฐานข้อมูลจริง (บอกผู้ใช้ว่าเป็นข้อมูลทดสอบ ต้องลบทีหลังถ้าจะเอาไปใช้งานจริง)

**Status:** [x] done

---

### Phase 3 — หน้าคิวรอตรวจ + หน้ารายละเอียด (ฝั่งหัวหน้า) อ่านของจริงได้ ไม่ต้อง login
**Goal:** ให้หัวหน้าเปิดลิงก์แล้วเห็นงานจริงที่ส่งมา อ่านแคปชันเต็ม กดลิงก์รูปเปิดได้ โดยไม่ต้องสมัครหรือ login

**How to test:**
1. เปิดหน้าคิวรอตรวจในเบราว์เซอร์อีกตัว (หรือโหมดไม่ล็อกอิน) เห็นชิ้นงานที่ส่งตรวจจาก Phase 2 อยู่บนสุด
2. คลิกเข้าไปดูรายละเอียด เห็นแคปชันเต็มไม่ตัด เห็นกำหนดลง ชื่อคนเขียน
3. กดลิงก์รูป เปิดแท็บใหม่ไปหน้ารูปจริง
4. ถ้าไม่มีงานรอตรวจเลย เห็นข้อความ "ตอนนี้ไม่มีงานรอตรวจ" ไม่ใช่หน้าว่าง

**Done when:**
- หน้าคิวและหน้ารายละเอียดดึงข้อมูลจริงจากฐานข้อมูล ไม่ใช้ mock data แล้ว
- เข้าถึงได้โดยไม่ต้อง login (เป็นลิงก์สาธารณะ)
- `npm run lint`, `npm run build` ผ่าน

**Status:** [x] done

---

### Phase 4 — ปุ่ม "ผ่าน" และ "ขอแก้" ใช้งานได้จริง (บังคับคอมเมนต์ตอนขอแก้)
**Goal:** ให้หัวหน้ากดตรวจได้จริงในหน้ารายละเอียด กดขอแก้ต้องพิมพ์เหตุผลก่อนเสมอ

**How to test:**
1. เปิดชิ้นงานที่ "รอตรวจ" กดปุ่ม "ขอแก้" โดยไม่พิมพ์อะไร — ระบบไม่ยอมให้กด เตือนให้พิมพ์ก่อน
2. พิมพ์คอมเมนต์แล้วกดขอแก้ — สถานะเปลี่ยนเป็น "ขอแก้" ชิ้นงานหายจากคิวรอตรวจ คอมเมนต์ติดอยู่กับเวอร์ชันนั้น
3. เปิดอีกชิ้นที่รอตรวจ กด "ผ่าน" โดยไม่พิมพ์คอมเมนต์ — ทำได้เลย สถานะเปลี่ยนเป็น "ผ่าน" หายจากคิว
4. ลองกดตรวจซ้ำชิ้นที่ตรวจไปแล้ว (จำลองสองคนกดพร้อมกัน) — ระบบบอกว่าชิ้นนี้ถูกตรวจไปแล้ว ไม่บันทึกทับ

**Done when:**
- ปุ่มผ่าน/ขอแก้ผูกกับเวอร์ชันล่าสุดที่ส่งตรวจ ไม่ใช่ข้อความที่แก้ค้างไว้
- มีระบบกันชนตรวจซ้ำ (เวอร์ชันนั้นถูกตรวจไปแล้วจะตรวจซ้ำไม่ได้)
- `npm run lint`, `npm run build` ผ่าน ทดสอบ 2 เคสข้างบนสำเร็จจริง

**Status:** [x] done

---

### Phase 5 — แก้ข้อความได้ตลอดเวลา + ส่งเวอร์ชันใหม่ให้ตรวจ (ประวัติเวอร์ชัน)
**Goal:** ให้เจ้าของแก้ข้อความได้ทุกเมื่อโดยไม่กระทบของที่หัวหน้ากำลังอ่าน จนกว่าจะกดส่งตรวจใหม่เอง

**How to test:**
1. เปิดชิ้นงานที่ "รอตรวจ" ในหลังบ้าน แก้ข้อความ กด "บันทึกไว้ก่อน" (ไม่ส่งตรวจ)
2. เปิดหน้าเดียวกันฝั่งหัวหน้า (ไม่ล็อกอิน) รีเฟรช — ยังเห็นข้อความเดิมที่ส่งไปตอนแรก ไม่ใช่ของที่เพิ่งแก้
3. กลับมาหลังบ้าน เห็นป้ายเตือน "มีการแก้ที่ยังไม่ได้ส่งตรวจ"
4. กด "ส่งเวอร์ชันใหม่ให้ตรวจ" — กลายเป็นเวอร์ชัน 2 ขึ้นบนสุดคิวหัวหน้าอีกครั้ง เวอร์ชัน 1 พร้อมคอมเมนต์เดิมยังอ่านได้ในประวัติ

**Done when:**
- ช่องเก็บ "ข้อความแก้ค้าง" แยกจากข้อความเวอร์ชันที่ส่งตรวจแล้วจริง ๆ ในฐานข้อมูล
- ประวัติเวอร์ชันแสดงครบทุกเวอร์ชันเก่าไม่ถูกทับ
- `npm run lint`, `npm run build` ผ่าน

**Status:** [x] done

---

### Phase 6 — "ลงแล้ว" + หน้าคลังงานที่ผ่านแล้ว + ตัวกรอง
**Goal:** ให้เจ้าของกดว่าเอาไปลงจริงแล้ว และให้ทีมย้อนดูงานเก่าเป็นตัวอย่างได้

**How to test:**
1. เปิดชิ้นงานสถานะ "ผ่าน" กดปุ่ม "ลงแล้ว" — สถานะเปลี่ยน หายจากหน้ารวมงานปกติ ไปโผล่ในคลังงานที่ผ่านแล้ว
2. เปิดหน้าคลังงานที่ผ่านแล้ว (ไม่ต้อง login) เลือกตัวกรองแบรนด์ + ช่องทาง — เห็นเฉพาะชิ้นที่ตรงกัน
3. เลือกตัวกรองที่ไม่มีข้อมูลตรงเลย — เห็นข้อความ "ไม่เจองานที่ตรงกับตัวกรองนี้"

**Done when:**
- คลังงานดึงเฉพาะสถานะ "ผ่าน" และ "ลงแล้ว" จริงจากฐานข้อมูล
- ตัวกรองทำงานจริง ไม่ใช่ mock
- `npm run lint`, `npm run build` ผ่าน

**Status:** [x] done

---

### Phase 7 — ลบชิ้นงาน (มีถามยืนยันก่อน)
**Goal:** ให้เจ้าของลบชิ้นงานที่ไม่ต้องการออกจากทุกหน้าได้ โดยมีการถามยืนยันก่อนเสมอ

**How to test:**
1. เปิดชิ้นงานใด ๆ ในหลังบ้าน กดปุ่ม "ลบ" — มีกล่องถามยืนยัน 1 ครั้ง
2. กดยืนยัน — ชิ้นงานหายจากหน้ารวมงาน หน้าคิว และหน้าคลัง ทันที
3. กดยกเลิกตอนถามยืนยัน — ชิ้นงานยังอยู่เหมือนเดิม

**Done when:**
- การลบเป็นการซ่อน (soft delete) ไม่ลบแถวจริงออกจากฐานข้อมูล ตามกฎ PRD
- `npm run lint`, `npm run build` ผ่าน

**Status:** [x] done

---

### Phase 8 — ระบบ login เจ้าของ (Neon Auth) ป้องกันหลังบ้าน
**Goal:** ให้เฉพาะเจ้าของ login เข้าหลังบ้านได้ คนอื่นเข้าไม่ได้แม้จะรู้ลิงก์

**How to test:**
1. เปิดลิงก์หลังบ้านโดยไม่ login — ถูกเด้งกลับไปหน้า login เสมอ
2. Login ด้วยอีเมล/รหัสผ่านของเจ้าของ — เข้าหลังบ้านได้ปกติ ทำทุกอย่างใน Phase 2-7 ได้เหมือนเดิม
3. ลองสมัครบัญชีที่สองด้วยอีเมลอื่น (ทดสอบเฉพาะตอนตั้งระบบ) แล้วยืนยันว่าไม่มีหน้าสมัครสมาชิกให้ใครมากดสมัครเองได้จากในเว็บ

**Done when:**
- ทำตามขั้นตอน Neon Auth อย่างเป็นทางการ (ใช้ skill `neon-auth-nextjs` ถ้ามี)
- หน้าฝั่งหัวหน้า/คลังงานยังคงเปิดได้โดยไม่ต้อง login เหมือนเดิม
- `npm run lint`, `npm run build` ผ่าน

**Status:** [x] done

---

### Phase 9 — ขึ้นเว็บจริง (Deploy)
**Goal:** เอาเว็บที่ทำเสร็จทั้งหมดขึ้นไปให้เปิดจากมือถือหรือคอมเครื่องไหนก็ได้ผ่านลิงก์จริง

**How to test:**
1. เปิดลิงก์เว็บจริงบนมือถือ เห็นหน้าคิวรอตรวจ
2. ลองทำ core loop ทั้งหมดซ้ำบนเว็บจริงอีกครั้ง (ส่งตรวจ → ผ่าน/ขอแก้ → ลงแล้ว)

**Done when:**
- `npm run build` ผ่านบนเครื่องก่อน push
- Push GitHub (ต้องได้ OK จากผู้ใช้ก่อน) → Vercel deploy สำเร็จ
- ตัวแปรที่ Neon integration ไม่ได้ใส่ให้ (เช่นค่า login) ถูกเพิ่มใน Vercel ตามขั้นตอนที่บอกผู้ใช้เอง ไม่ผ่านแชท

**Status:** [ ] not started

---

## การครอบคลุมเช็กลิสต์ "v1 ถือว่าเสร็จเมื่อ…" (PRD หัวข้อ 9)

| ข้อ PRD | ครอบคลุมในเฟส |
|---|---|
| 1. login เข้าหลังบ้านได้ / ไม่ login เข้าไม่ได้ | Phase 8 |
| 2. เพิ่มแคปชัน + ส่งตรวจ ขึ้นบนคิวทันที | Phase 2 + 3 |
| 3. หัวหน้าเห็นของจริง เปิดรูปได้ / แก้ค้างไม่กระทบของที่ส่งไปแล้ว | Phase 3 + 5 |
| 4. ขอแก้ไม่พิมพ์คอมเมนต์ไม่ได้ | Phase 4 |
| 5. แก้แล้วส่งใหม่เป็นเวอร์ชัน 2 เวอร์ชัน 1 ยังอยู่ | Phase 5 |
| 6. ผ่าน → ลงแล้ว → ไปคลังงาน | Phase 4 + 6 |
| 7. กรองคลังงาน + ข้อความตอนไม่มีข้อมูล | Phase 6 |

ทุกข้อใน PRD หัวข้อ 9 มีเฟสรองรับครบ ไม่มีข้อไหนตกหล่น

## การตรวจสอบเมื่อทำเสร็จแต่ละเฟส

ทุกเฟสจบด้วย: `npm run lint`, `npm run build` ผ่านทั้งคู่ + ทำตามขั้นตอน "How to test" ด้วยตัวเองจนครบ แล้วจึงรายงานผู้ใช้เป็นภาษาไทยง่าย ๆ ตามฟอร์แมตใน CLAUDE.md (ทำอะไรได้แล้ว / ทดสอบยังไง / ขั้นต่อไปคืออะไร) ก่อน commit และรอ OK ก่อนเริ่มเฟสถัดไป
